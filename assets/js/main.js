/**
 * Main JavaScript Entry Point
 * Handles initialization of all modules
 */

document.addEventListener('DOMContentLoaded', () => {
    console.log('%c👋 Hey there! Looking at the code? Nice to meet you! 🚀', 'background: #2b2b2b; color: #bada55; padding: 6px; font-size: 14px; border-radius: 4px;');
    console.log('%cFeel free to reach out if you have any questions or just want to say hi!', 'color: #aaa; font-style: italic;');
    console.log('Portfolio initialized');

    // Initialize custom UI elements (right click, etc)
    // Already handled in custom-ui.js

    // Initialize i18n
    // Already handled in i18n.js

    // Initialize Matrix Effect
    // Already handled in matrix.js

    // Initialize Contact Form
    // Already handled in contact.js

    // Initialize Projects Carousel
    // Already handled in projects-carousel.js

    // Initialize Parallax
    // Already handled in parallax.js

    // Initialize Nav Scroll
    // Already handled in nav-scroll.js
});
